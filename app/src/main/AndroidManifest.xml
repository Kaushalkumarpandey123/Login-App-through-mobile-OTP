<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebasedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Firebasedemo">
        <activity
            android:name=".new_password"
            android:exported="true"
            android:parentActivityName=".otp_forgot_password"/>
        <activity
            android:name=".otp_forgot_password"
            android:exported="true"
            android:parentActivityName=".forgot_password"/>
        <activity
            android:name=".forgot_password"
            android:exported="true"
            android:parentActivityName=".login"/>
        <activity
            android:name=".enter_otp_screen"
            android:exported="true" />
        <activity
            android:name=".dasbord"
            android:exported="true"
            android:parentActivityName=".login" />
        <activity
            android:name=".create_account"
            android:exported="true"
            android:parentActivityName=".login" />
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>